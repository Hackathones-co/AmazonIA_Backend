# SALA Galápagos Backend — Dependencies
# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ML inference
torch==2.5.1
numpy==2.2.1

# Data processing
pandas==2.2.3
scipy==1.14.1

# HTTP client (for Open-Meteo, Claude API)
httpx==0.28.1

# Voice (ElevenLabs Conversational AI)
elevenlabs>=1.50.0      # Conversational AI SDK (Conversation class + signed-URL helper)
websockets>=13.0        # WebSocket relay to ElevenLabs WSS

# Optional: Redis cache
# redis==5.2.1

# Dev
pytest==8.3.4
pytest-asyncio==0.25.0
